export const FLOATING_NUMBERS_CONFIG = {
  duration: 250, // Animation duration in milliseconds
  travelDistance: 20, // Vertical distance traveled in pixels
  horizontalDrift: 30, // Maximum horizontal drift in pixels
  fontSizes: {
    damage: '3xl',
    heal: '2xl',
    xp: 'xl',
    gold: 'xl',
  },
} as const
